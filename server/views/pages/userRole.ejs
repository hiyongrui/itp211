<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/7.26.10/sweetalert2.all.js"> </script>
</head>
<style>
    #output {
        border-radius: 50%;
        /*display: inline; */
        width: 140px;
        height: 140px;
    }

    footer.page-footer {
        margin-top: 400px !important;
    }
</style>

<body>
    <% include ../partials/header %>

        <!-- Modal -->
        
         <form id="submitUserRole" action="/userRole" method="post">  

            <h1> Welcome
                <%=user.name%> , please select your account! </h1>
            <button type ="submit" class="btn btn-primary" name="role" value="user" onclick="uploadImageSwal(this)"> I WAN TO BE A USER! </button>
            <button type="submit" class="btn btn-primary" name="role" value="instructor"> I WAN TO BE A INSTRUCTOR! </button>

         </form>  




        <% include ../partials/footer %>


            <script>
                var loadFile = function (event) {
                    var output = document.getElementById('output');
                    output.src = URL.createObjectURL(event.target.files[0]);
                };

                /*
                async function uploadImageSwal(this_role) {

                    var data = $(this_role).val();

                    console.log("data of this " + data);
                

                    const { value: file } = await swal({
                        title: 'Select image',
                        input: 'file',
                        inputAttributes: {
                            'accept': 'image/*',
                            'aria-label': 'Upload your profile picture'
                        }
                    })
                    if (file) {
                        const reader = new FileReader
                        reader.onload = (e) => {
                            swal({
                                title: 'Your uploaded picture',
                                imageUrl: e.target.result,
                                imageAlt: 'The uploaded picture'
                            }).then(confirm => {
                                if (confirm) {
                                    alert("SBMIT");
                                    alert(JSON.stringify(reader));
                                    $.ajax({
                                        url: "localhost:3000/userUpload",
                                        method: "POST",
                                        data: {
                                            
                                        }
                                    })
                                }
                            })
                        }
                        reader.readAsDataURL(file)
                    }
                    
                } */

            </script>

</body>

</html>